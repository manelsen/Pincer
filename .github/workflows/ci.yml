name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    name: Build and Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: pincer
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      MIX_ENV: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Elixir and OTP
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.18.0"
          otp-version: "27.2"

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-otp27-elixir1.18-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-otp27-elixir1.18-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile dependencies
        run: |
          # Compile prod first so prod artifacts exist before test's cross-env check runs
          MIX_ENV=prod mix deps.compile --force
          # Then compile test (pincer_whatsapp cross-env check finds fresh prod artifacts)
          mix deps.compile --force

      - name: Create and migrate database
        run: |
          mix ecto.create
          mix ecto.migrate

      - name: Run QA gate (format + compile + test)
        run: mix qa
