# 2026-02-22

- Implemented `SPR-051` (channel A11y baseline) with doc-first + TDD:
  - Spec + backlog:
    - Added `SPR-051` section in `SPECS.md` for route shortcuts, short hints, and keyboard/screen-reader oriented menu guidance.
    - Marked sprint completion in `TODO.md`.
  - Core UX contract:
    - `Pincer.Core.UX` now provides `resolve_shortcut/1` to resolve textual shortcuts with and without `/` (`menu`, `status`, `models`, `ping`) and keep compatibility for `Menu`, `/help`, `/commands`.
    - `help_text/1` now includes explicit route guidance for command usage with/without `/`.
    - unknown hints kept short and action-oriented around `/menu`.
  - Channel parity:
    - `Pincer.Channels.Telegram.UpdatesProvider` now routes validated shortcut text through command handlers before normal session processing.
    - `Pincer.Channels.Discord.Consumer` now routes validated shortcut text through command handlers before normal session processing.
  - Tests:
    - Added UX unit tests for shortcut resolution and concise hints in `test/pincer/core/ux_test.exs`.
    - Added channel regression tests for plain-text `status` routing in:
      - `test/pincer/channels/telegram_test.exs`
      - `test/pincer/channels/discord_test.exs`
  - Validation:
    - `mix test test/pincer/core/ux_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs` => green (`52 tests`, `0 failures`).
    - `mix test` => green (`3 doctests`, `356 tests`, `0 failures`).

- Implemented `SPR-050` (tool-surface hardening) with doc-first + TDD:
  - Spec + backlog:
    - Added sprint scope and acceptance criteria in `SPECS.md` (`Hardening de Superfície de Ferramentas`).
    - Marked `SPR-050` as complete in `TODO.md`.
  - `Pincer.Tools.FileSystem` hardening:
    - added existing-ancestor symlink resolution checks to block workspace jail escape via symlink chains.
  - `Pincer.Tools.SafeShell` hardening:
    - unsafe path patterns now require approval in both path args and generic args (`/`, `~`, `..`, null-byte, drive-prefix).
  - `Pincer.Tools.Web` hardening:
    - normalized host validation (including trailing-dot hosts),
    - safe IPv4/IPv6 private-range handling (including IPv4-mapped IPv6),
    - DNS resolution blocking for hosts that resolve to internal/private addresses.
  - Tests:
    - Added regressions in `test/security_audit_test.exs` for symlink escape, absolute path arguments, trailing-dot localhost, and IPv4-mapped IPv6.
  - Validation:
    - `mix test test/security_audit_test.exs test/pincer/core/security_audit_test.exs test/mix/tasks/pincer.security_audit_test.exs` => green (`48 tests`, `0 failures`).
    - `mix test` => green (`3 doctests`, `351 tests`, `0 failures`).

- Implemented `SPR-049` (operability expansion for MCP dynamic config loading) with doc-first + TDD:
  - Added dynamic MCP config loader:
    - `lib/pincer/connectors/mcp/config_loader.ex`
  - Loader capabilities:
    - supports `mcpServers` (Cursor/Claude Desktop) and `mcp.servers` shapes;
    - supports configurable source list via `:pincer, :mcp_dynamic_config_paths` (or explicit paths in tests);
    - ignores missing/invalid JSON files without crashing startup;
    - filters dynamic servers flagged with `disabled: true`;
    - merges configs with deterministic precedence (`config.yaml` static server entries override dynamic collisions).
  - MCP manager integration:
    - `lib/pincer/connectors/mcp/manager.ex` now resolves server map through `resolve_servers_config/2` before boot.
  - Added tests:
    - `test/pincer/connectors/mcp/config_loader_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs` (dynamic merge + static precedence)
  - Validation:
    - focused suites green (`10 tests`, 0 failures).

- Sprint renumbering note (retroactive strict isolation):
  - legacy `SPR-01..SPR-04` -> `SPR-028`
  - legacy `SPR-05..SPR-14` -> `SPR-029`
  - legacy `SPR-15` -> `SPR-030`
  - legacy `SPR-16` -> `SPR-031`

- Implemented `SPR-041` (`C05`) with doc-first + TDD:
  - Added core interaction payload policy:
    - `lib/pincer/core/channel_interaction_policy.ex`
  - Telegram hardening:
    - `lib/pincer/channels/telegram.ex` now parses callback payloads via core policy and filters oversized callback buttons.
    - malformed/invalid callback payloads now follow friendly fallback path instead of attempting invalid model selection.
  - Discord hardening:
    - `lib/pincer/channels/discord.ex` now tolerates malformed interactions (missing `data.custom_id`) and parses actions via core policy.
    - `/models` interaction now filters provider/model `custom_id` payloads above Discord limit.
  - Added tests:
    - `test/pincer/core/channel_interaction_policy_test.exs`
    - `test/pincer/channels/telegram_test.exs` (oversized provider callback filtering + invalid `select_model` shape)
    - `test/pincer/channels/discord_test.exs` (missing `custom_id` fallback + oversized provider `custom_id` filtering)
  - Validation:
    - focused suites green (`47 tests`);
    - full suite green (`mix test` => `3 doctests, 309 tests, 0 failures`).

- Implemented `SPR-042` (`C01`) with doc-first + TDD:
  - Added onboarding preflight + merge contracts in core:
    - `Pincer.Core.Onboard.preflight/1`
    - `Pincer.Core.Onboard.merge_config/2`
  - Preflight validations now fail early with actionable hints for:
    - invalid `database.database` paths (empty, absolute, or traversal);
    - missing `llm.provider`;
    - missing default model for selected provider.
  - `mix pincer.onboard` hardening:
    - now merges existing `config.yaml` with defaults before applying overrides;
    - validates option matrix (`--db-path`/`--provider`/`--model` require `config_yaml` when capabilities are scoped);
    - runs preflight and aborts with explicit hint list on failure.
  - Added tests:
    - `test/pincer/core/onboard_test.exs` (preflight + deep merge coverage)
    - `test/mix/tasks/pincer.onboard_test.exs` (matrix validation, preflight failure, existing config merge preservation)
  - Validation:
    - focused suites green (`17 tests`);
    - full suite green (`mix test` => `3 doctests, 316 tests, 0 failures`).

- Implemented `SPR-043` (`C15`) with doc-first + TDD:
  - Extended MCP HTTP transport for streamable/SSE responses:
    - `lib/pincer/connectors/mcp/transports/http.ex`
  - New behaviors:
    - parses `text/event-stream` payloads into incremental JSON-RPC messages;
    - ignores SSE sentinel `data: [DONE]`;
    - returns explicit `{:error, {:invalid_sse_data, ...}}` on malformed SSE data;
    - supports optional cleanup callback in `close/1` for safe transport lifecycle.
  - Added tests:
    - `test/pincer/connectors/mcp/transports/http_test.exs` (SSE parse/forward, malformed SSE, close callback path)
  - Validation:
    - focused MCP suites green (`11 tests`);
    - full suite green (`mix test` => `3 doctests, 319 tests, 0 failures`).

- Implemented `SPR-044` (`C16`) with doc-first + TDD:
  - Hardened skills installation trust boundary in core:
    - `lib/pincer/core/skills.ex`
  - New enforcement:
    - explicit install opt-in (`allow_install: true`);
    - secure source validation (default `https` scheme + host required);
    - wildcard host allowlist support (`*.trusted.example.com`);
    - sandbox root symlink rejection before install path resolution.
  - Added/updated tests:
    - `test/pincer/core/skills_test.exs` (opt-in requirement, non-https rejection, wildcard host acceptance, symlink sandbox block)
  - Validation:
    - focused skills suites green (`10 tests`);
    - full suite green (`mix test` => `3 doctests, 323 tests, 0 failures`).

- Defined core-first integration strategy for OpenClaw/PicoClaw/NanoBot capabilities with explicit hexagonal boundaries.
- Delivered UX baseline in core (`Pincer.Core.UX`) and aligned Telegram/Discord command ergonomics around `/menu`.
- Implemented onboarding MVP foundation:
  - `mix pincer.onboard` task (interactive + `--non-interactive` mode).
  - Core onboarding service (`Pincer.Core.Onboard`) with deterministic operation plan/apply flow.
- Migrated default DB paths to `./db` in `config/dev.exs`, `config/test.exs`, and `config.yaml`.
- Added TDD coverage for onboarding and DB path contract:
  - `test/pincer/core/onboard_test.exs`
  - `test/mix/tasks/pincer.onboard_test.exs`
  - `test/pincer/config/db_paths_test.exs`
- Known baseline instability remains in full suite:
  - MCP manager discovery timeouts in some runs.
  - One hot-swap integration timing failure in `test/pincer/llm/hot_swap_integration_test.exs`.
- Production bug observed and fixed:
  - Telegram callback flow (`/models` inline buttons) was crashing `UpdatesProvider` due to wrong API adapter arity.
  - Fixed callback calls to use `edit_message_text/4` in `lib/pincer/channels/telegram.ex`.
  - Added regression coverage in `test/pincer/channels/telegram_test.exs` to ensure callback path invokes `edit_message_text/4`.
- Warning reduction pass started:
  - Grouped `handle_info/2` clauses in channel session workers.
  - Completed missing test adapter callbacks for Telegram/Discord.
  - Replaced deprecated `use Bitwise` with `import Bitwise` in web tool.
  - Added `stream_completion/4` implementations for provider adapters that previously only implemented `chat_completion/4`.
- Added centralized friendly-error mapping:
  - `lib/pincer/core/error_ux.ex` with mappings for common HTTP/network/internal/db errors.
  - Session integration to avoid silent failures:
    - executor failure path now returns user-friendly `agent_error`
    - quick-reply failure path now returns friendly fallback response
  - Coverage added in `test/pincer/core/error_ux_test.exs`.
- Extended retry/backoff policy in `Pincer.LLM.Client`:
  - keeps exponential backoff for `429`
  - adds transient retry for `408/429/500/502/503/504` and transport-level transient failures
  - honors `retry_after_ms` metadata when available
  - adds jitter and configurable runtime policy via `:llm_retry`
  - coverage in `test/pincer/llm/retry_policy_test.exs`
- Hardened Telegram callback loop to avoid poller crashes:
  - `UpdatesProvider` now wraps update processing with guarded error handling.
  - malformed `callback_query` payloads are ignored with warning logs (no crash).
  - unknown callback actions respond with user guidance + persistent `Menu` keyboard.
  - `edit_message_text` failures now trigger friendly fallback message instead of silent failure.
  - coverage added in `test/pincer/channels/telegram_test.exs` for malformed/unknown/edit-failure callbacks.
- Updated `SPECS.md` with callback resilience acceptance criteria.
- Updated `TODO.md` with current actionable checklist aligned to hexagonal integration + UX/DX/A11y.
- Consolidated capability-gap analysis across `analysis/openclaw`, `analysis/picoclaw`, and `analysis/hkuds_nanobot`.
- Added execution-grade matrix in `docs/capability_matrix.md`:
  - 18 capabilities (`C01..C18`) with source evidence, current Pincer status, gap, target core port/module, and first TDD slice.
  - priority waves (`P0/P1/P2`) and immediate sprint suggestions (`SPR-028..SPR-029`).
- Linked prior checklist doc to the new matrix:
  - `docs/SPECS/CAPABILITY_CHECKLIST_V1.md` now points to `docs/capability_matrix.md`.
- Updated `TODO.md` to mark matrix publication as done and align immediate backlog with capability IDs.
- Implemented `SPR-028` (`C03`) with doc-first + TDD:
  - Added core command governance policy: `lib/pincer/core/ux/menu_policy.ex`.
  - Policy now enforces per-channel validation, normalization, dedupe, and command cap.
  - Integrated policy in adapters:
    - `lib/pincer/channels/telegram.ex` (`register_commands/0`)
    - `lib/pincer/channels/discord.ex` (`register_commands/0`)
  - Added tests:
    - `test/pincer/core/ux/menu_policy_test.exs`
  - Focused suites passing:
    - `test/pincer/core/ux/menu_policy_test.exs`
    - `test/pincer/channels/telegram_test.exs`
    - `test/pincer/channels/discord_test.exs`
- Added shared glossary file for planning/architecture acronyms:
  - `GLOSSARIO.md` (SPR, Cxx, ADR, P0/P1/P2, TDD/DDD/RED-GREEN-REFACTOR, UX/DX/A11y).
- Implemented `SPR-028` (`C06`) with doc-first + TDD:
  - Added core DM policy engine: `lib/pincer/core/access_policy.ex`.
  - Policy supports `open`, `allowlist` (with wildcard `*` and prefix), `disabled`, and `pairing`.
  - Telegram integration:
    - private chats (`chat.type == "private"`) are authorized by core policy before session routing.
    - denied DMs receive friendly message + persistent `Menu` keyboard.
  - Discord integration:
    - DM events (`guild_id == nil`) are authorized by core policy before session routing.
    - denied DMs receive friendly message in-channel.
  - Channel configs are exposed for policy evaluation:
    - `:telegram_channel_config` and `:discord_channel_config` in app env.
  - Added tests:
    - `test/pincer/core/access_policy_test.exs`
    - `test/pincer/channels/telegram_test.exs` (DM policy block path)
    - `test/pincer/channels/discord_test.exs` (DM policy block path)
  - Focused suites passing:
    - `mix test test/pincer/core/access_policy_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs`
- Implemented `SPR-028` (`C09`) with doc-first + TDD:
  - Added core model registry module:
    - `lib/pincer/core/models/registry.ex`
  - Registry capabilities (read-only v1):
    - provider list in stable order
    - model list merge (`default_model` + `models`/`model_list`) with dedupe/filter
    - alias resolution via `model_aliases`/`aliases`
  - Integrated channel-facing API through `Pincer.LLM.Client`:
    - `list_providers/0` now delegates to core registry
    - `list_models/1` now delegates to core registry
  - Added tests:
    - `test/pincer/core/models/registry_test.exs`
    - `test/pincer/llm/client_test.exs` (registry integration assertions)
  - Focused suites passing:
    - `mix test test/pincer/core/models/registry_test.exs test/pincer/llm/client_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs`
- Dependency warning cleanup (local environment):
  - Patched `deps/websockex` deprecation/charlist warnings (`mix.exs`, `utils.ex`, `frame.ex`, `websockex.ex`).
  - `mix deps.compile websockex --force` now compiles without the previous warnings shown by user.
- Implemented `SPR-028` (`C17`) with doc-first + TDD:
  - Added core streaming policy:
    - `lib/pincer/core/streaming_policy.ex`
  - Policy enforces:
    - preview with cursor only during streaming
    - finalization without cursor
    - in-place final edit when preview message exists (no duplicate final send)
  - Integrated policy into channel session workers:
    - `lib/pincer/channels/telegram/session.ex`
    - `lib/pincer/channels/discord/session.ex`
  - Added tests:
    - `test/pincer/core/streaming_policy_test.exs`
    - `test/pincer/channels/telegram_session_test.exs`
    - `test/pincer/channels/discord_session_test.exs`
  - Focused suites passing:
    - `mix test test/pincer/core/access_policy_test.exs test/pincer/core/models/registry_test.exs test/pincer/core/streaming_policy_test.exs test/pincer/channels/telegram_session_test.exs test/pincer/channels/discord_session_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs test/pincer/llm/client_test.exs`
- Implemented `SPR-029` (`C12`) first operational slice:
  - Telegram polling hardening with exponential backoff state (`offset` + `failures`) in `Pincer.Channels.Telegram.UpdatesProvider`.
  - Added tests for backoff and recovery lifecycle:
    - `next_poll_interval/1` cap and fallback behavior
    - failure increments `failures` without offset advance
    - success after failure resets `failures` and advances offset
  - Added `systemd` deployment artifacts:
    - `infrastructure/systemd/pincer.service`
    - `infrastructure/systemd/pincer.env.example`
    - `docs/systemd.md` runbook
  - Focused regression suite passed (56 tests).
  - Full `mix test` still has pre-existing instability outside sprint scope:
    - `test/pincer/llm/retry_policy_test.exs` (`Retry-After` date timing assertion flaky)
    - `test/pincer/llm/hot_swap_integration_test.exs` (backoff timing assert)
    - `test/pincer/core/executor_streaming_test.exs` (MCP manager timeout)
- Implemented `SPR-029` stabilization slice (daemon-readiness):
  - Hardened tool registry against MCP outages:
    - `lib/pincer/adapters/native_tool_registry.ex` now uses safe fallback (`[]` MCP tools) on manager timeout/exit.
    - configurable MCP manager module via `:pincer, :mcp_manager` for tests.
  - Hardened MCP manager tool lookup timeout path:
    - `lib/pincer/connectors/mcp/manager.ex` `get_all_tools/1` now catches timeout exits and returns `[]`.
    - default timeout set to `200ms` to avoid 5s stalls in executor path.
  - Added TDD coverage:
    - `test/pincer/adapters/native_tool_registry_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
  - Retested previously flaky failures:
    - `test/pincer/llm/hot_swap_integration_test.exs` now passes.
    - `test/pincer/core/executor_streaming_test.exs` now passes.
  - Full suite now green:
    - `mix test` => `181 tests, 0 failures`.
- Implemented `SPR-029` streaming/context hardening:
  - Root cause identified in live logs:
    - stream path could propagate invalid stream payloads (`Protocol.UndefinedError Enumerable/Collectable`);
    - provider-side completion token default could overflow model context on long histories.
  - Stabilization changes:
    - `lib/pincer/core/executor.ex` now falls back to `chat_completion` when streaming fails or stream payload is invalid.
    - `lib/pincer/llm/client.ex` now validates stream shape and falls back to single-shot chat for malformed stream responses.
    - `lib/pincer/llm/providers/openai_compat.ex` now:
      - uses single-shot completion for stream adapter output (synthetic one-chunk stream),
      - applies context-aware completion token budget (`max_tokens` / `max_completion_tokens`) with explicit clamp,
      - supports `extra_body` merge in payload building.
    - `lib/pincer/core/error_ux.ex` now maps context-overflow `400` to actionable guidance (`/reset` or larger-context model).
  - New tests:
    - `test/pincer/llm/providers/openai_compat_test.exs`
    - `test/pincer/core/error_ux_test.exs` (context overflow mapping)
    - `test/pincer/llm/client_test.exs` (invalid stream fallback)
  - Validation:
    - focused suites green;
    - full suite green: `mix test` => `3 doctests, 186 tests, 0 failures`.
- Implemented `SPR-029` (`C12`) error taxonomy + telemetry:
  - Added stable error classifier:
    - `lib/pincer/core/error_class.ex`
  - Added telemetry facade:
    - `lib/pincer/core/telemetry.ex`
  - Integrated telemetry/classification into:
    - `lib/pincer/llm/client.ex` (retry + terminal failures)
    - `lib/pincer/session/server.ex` (executor/quick-reply failures)
    - `lib/pincer/channels/telegram.ex` (poller failures)
  - Error UX expanded for stream protocol failures:
    - `lib/pincer/core/error_ux.ex`
  - Added tests:
    - `test/pincer/core/error_class_test.exs`
    - `test/pincer/core/telemetry_test.exs`
    - `test/pincer/llm/client_telemetry_test.exs`
    - `test/pincer/core/error_ux_test.exs` (stream protocol mapping)
    - `test/pincer/channels/telegram_test.exs` (warning-level poll log)
- Implemented `SPR-029` DX ergonomics slice:
  - Added DX macros:
    - `lib/pincer/dx/macros.ex` (`with_app_env`, `assert_ok`)
  - Added mix aliases for quick feedback loops:
    - `mix.exs` aliases: `qa`, `test.quick`, `sprint.check`
  - Added tests:
    - `test/pincer/dx/macros_test.exs`
    - `test/mix/aliases_test.exs`
- Validation after SPR-029 slices (error taxonomy + DX):
  - full suite green:
    - `mix test` => `3 doctests, 202 tests, 0 failures`.
- Implemented `SPR-029` (`C04/C05`) Discord ergonomics parity + interaction fallback hardening:
  - `Menu` textual shortcut now maps to `/menu` in Discord message flow.
  - `/models` flow now includes explicit `Menu` fallback button (`show_menu`) in both slash and text command paths.
  - Unknown/invalid Discord interactions no longer crash:
    - unknown `custom_id` now returns friendly guidance from core UX.
    - malformed `select_model` payload now returns guidance instead of `MatchError`.
  - Core UX contract expanded:
    - `Pincer.Core.UX.unknown_interaction_hint/0`.
  - Tests:
    - `test/pincer/channels/discord_test.exs` (Menu shortcut, models menu button, unknown/malformed interaction fallback).
    - `test/pincer/core/ux_test.exs` (`unknown_interaction_hint/0`).
- Implemented `SPR-029` (core ports formalization):
  - Added new core ports:
    - `lib/pincer/core/ports/onboarding.ex`
    - `lib/pincer/core/ports/capability_discovery.ex`
    - `lib/pincer/core/ports/user_menu.ex`
  - Added core capability catalog:
    - `lib/pincer/core/capability_discovery.ex` (baseline capabilities `C01`, `C03`, `C04`, `C06`, `C09`, `C12`, `C17`).
  - Core modules now declare explicit behavior contracts:
    - `Pincer.Core.Onboard` -> `Pincer.Core.Ports.Onboarding`
    - `Pincer.Core.UX` -> `Pincer.Core.Ports.UserMenu`
  - Tests:
    - `test/pincer/core/capability_discovery_test.exs`
    - `test/pincer/core/ports_contract_test.exs`
- Implemented `SPR-029` (adapter/provider contract tests):
  - Added channel adapter contract suite:
    - `test/pincer/contracts/channel_adapter_contract_test.exs`
  - Added LLM provider contract suite:
    - `test/pincer/contracts/llm_provider_contract_test.exs`
  - Contract tests verify exported callback surface and behavior declaration expectations without external network dependency.
- Updated backlog/docs:
  - `TODO.md` marked multiple `SPR-029` slices as done and updated immediate checklist.
  - `docs/capability_matrix.md` updated status for `C04` and `C05` after Discord parity/hardening.
- Validation after additional SPR-029 slices (Discord ergonomics + ports + contracts):
  - full suite green:
    - `mix test` => `3 doctests, 216 tests, 0 failures`.
- Implemented `SPR-029` (`C01`) onboarding capability model:
  - `Pincer.Core.Onboard` now supports explicit capability planning:
    - `available_capabilities/0`
    - `plan/2` with `capabilities: [...]`
  - Supported capabilities v1:
    - `workspace_dirs`
    - `memory_file`
    - `config_yaml`
  - Validation behavior:
    - unknown capabilities return `{:error, {:unknown_capabilities, [...]}}`.
  - `plan/1` remains backward compatible (uses full capability set).
  - `mix pincer.onboard` now supports:
    - `--capabilities workspace_dirs,config_yaml`
  - Mix task raises explicit validation error for invalid capability IDs.
  - Tests:
    - `test/pincer/core/onboard_test.exs` (capability list, scoped planning, invalid capability).
    - `test/mix/tasks/pincer.onboard_test.exs` (capability-limited run and invalid capability error).
- Validation after SPR-029:
  - full suite green:
    - `mix test` => `3 doctests, 221 tests, 0 failures`.
- Implemented `SPR-029` (`C10/C12`) unified retry/transient policy in core:
  - Added central policy module:
    - `lib/pincer/core/retry_policy.ex`
  - Core policy now owns:
    - request-level `retryable?/1` matrix for HTTP/transient transport/timeouts;
    - operational `transient?/1` classification for warning vs error logging;
    - `Retry-After` normalization/parsing (`retry_after_ms/3`, `parse_retry_after/2`).
  - Integrations updated:
    - `lib/pincer/llm/client.ex` now delegates retryability + Retry-After handling to core policy.
    - `lib/pincer/session/server.ex` now uses core `transient?/1` for executor failure log level.
    - `lib/pincer/channels/telegram.ex` (`UpdatesProvider`) now uses core `transient?/1` for poller log level.
  - Tests:
    - Added `test/pincer/core/retry_policy_test.exs`.
    - Stabilized HTTP-date retry timing in `test/pincer/llm/retry_policy_test.exs` (reduced second-precision flakiness).
  - Validation:
    - focused suites green:
      - `mix test test/pincer/core/retry_policy_test.exs test/pincer/llm/retry_policy_test.exs test/pincer/llm/client_telemetry_test.exs test/pincer/channels/telegram_test.exs`
- Documentation sync after `SPR-029`:
  - Added `Unreleased` section in `CHANGELOG.md` with added/changed/fixed notes for centralized retry/transient policy.
  - `docs/capability_matrix.md` already reflected current `C12` wording in the working tree (no additional diff required).
- Capability matrix quality pass:
  - `docs/capability_matrix.md` evolved from draft style to execution-oriented plan (`Living v2`).
  - Added status legend + execution snapshot (`done`/`partial`/`missing`) and explicit critical path (`C12 -> C10 -> C11`).
  - Updated rows for `C01` and `C10` with current sprint context.
  - Added dependency chains (`C12->C10->C11` and `C06->C07->C08`) and made next sprints (`SPR-030..SPR-032`) concrete with interface/test intent.
- System docs alignment pass after matrix refresh:
  - `TODO.md` updated with explicit pending sprints `SPR-030`, `SPR-031`, `SPR-032`.
  - `SPECS.md` now includes `SPR-030` spec section (`C10+C12`) and references detailed doc.
  - Added `docs/SPECS/FAILOVER_POLICY_V1.md` with interfaces, decision matrix, integration and TDD plan.
  - `docs/SPECS/CAPABILITY_CHECKLIST_V1.md` rewritten as compact living index aligned with `docs/capability_matrix.md` (removed stale status table).
- Implemented `SPR-030` (`C10+C12`) deterministic failover v1:
  - Added core policy module:
    - `lib/pincer/core/llm/failover_policy.ex`
  - Policy capabilities:
    - deterministic `next_action/2` (`retry_same` / `fallback_model` / `fallback_provider` / `stop`)
    - deterministic routing state transitions + `summarize_attempts/1`
  - `LLM.Client` integration:
    - propagates failover state across attempts;
    - delegates terminal retryable decisions to `FailoverPolicy.next_action/2`;
    - executes deterministic model/provider fallback before terminal stop.
  - New tests:
    - `test/pincer/core/llm/failover_policy_test.exs`
    - `test/pincer/llm/failover_integration_test.exs`
  - Validation:
    - focused suites green (failover + LLM regression);
    - full suite green:
      - `mix test` => `3 doctests, 234 tests, 0 failures`.
- Docs updated after `SPR-030`:
  - `TODO.md` now marks `SPR-030` as done.
  - `CHANGELOG.md` (`Unreleased`) includes failover policy/module and client integration notes.
  - `docs/capability_matrix.md` updated with `C10` v1 delivery status and next execution window (`SPR-031..SPR-033`).
- Implemented `SPR-031` (`C11`) cooldown cross-request por provider:
  - Added core store:
    - `lib/pincer/core/llm/cooldown_store.ex`
  - `FailoverPolicy` integration:
    - provider fallback now filters candidates with `CooldownStore.available_providers/1`.
  - `LLM.Client` integration:
    - applies provider cooldown on terminal transient failures;
    - clears provider cooldown on successful usage;
    - routes around default provider when it is cooling down and a healthy alternative exists.
  - New/updated tests:
    - `test/pincer/core/llm/cooldown_store_test.exs`
    - `test/pincer/core/llm/failover_policy_test.exs`
    - `test/pincer/llm/failover_integration_test.exs`
  - Validation:
    - focused LLM/cooldown suites green;
    - full suite green:
      - `mix test` => `3 doctests, 239 tests, 0 failures`.
- Docs alignment after `SPR-031`:
  - `TODO.md` marks `SPR-031` as done.
  - `CHANGELOG.md` (`Unreleased`) now includes cooldown store + cooldown-aware routing.
  - `docs/capability_matrix.md` and `docs/SPECS/CAPABILITY_CHECKLIST_V1.md` updated to reflect `C11` as delivered and shift execution window to `SPR-032..SPR-034`.
  - `docs/SPECS/COOLDOWN_STORE_V1.md` moved from "approved" to "implemented" status.
- Implemented `SPR-045` (`C01`) first delivery slice with doc-first + TDD:
  - Added spec and contracts:
    - `docs/SPECS/ONBOARD_REMOTE_ASSISTED_V1.md`
    - `SPECS.md` (`SPR-045` section with interfaces and acceptance criteria)
  - Core onboarding expansion:
    - `Pincer.Core.Onboard.assisted_preflight/2`
    - `Pincer.Core.Onboard.remote_assisted_plan/2`
  - Assisted preflight now reports environment checklist for:
    - enabled channel token env readiness;
    - selected provider credential env readiness (`env_key`);
    - MCP command availability in PATH.
  - Remote-assisted onboarding plan now validates:
    - required remote host;
    - absolute remote path without traversal.
  - `mix pincer.onboard` remote mode:
    - new flags: `--mode`, `--remote-host`, `--remote-user`, `--remote-path`;
    - remote mode prints assisted checklist + deterministic SSH bootstrap steps;
    - remote mode does not run local `apply_plan/2` (no local file mutation).
  - Tests added/updated:
    - `test/pincer/core/onboard_test.exs`
    - `test/mix/tasks/pincer.onboard_test.exs`
  - Validation:
    - focused onboarding suites green (`24 tests, 0 failures`).
  - Docs/backlog sync:
    - `TODO.md` marks `SPR-045` as done;
    - `CHANGELOG.md` updated with remote/assisted onboarding notes;
    - `docs/capability_matrix.md` and `docs/SPECS/CAPABILITY_CHECKLIST_V1.md` updated (`C01` now `done`, critical path becomes `C15`).
- Implemented `SPR-046` (`C15`) with doc-first + TDD:
  - Added spec section in `SPECS.md` for long-lived MCP HTTP stream resilience:
    - heartbeat filtering;
    - reconnect/backoff controls;
    - dedupe after reconnect.
  - Transport hardening in `lib/pincer/connectors/mcp/transports/http.ex`:
    - retries transient SSE interruptions (`stream closed without [DONE]`, transport timeout/closed, retryable `4xx/5xx`);
    - exponential backoff with configurable bounds;
    - `Last-Event-ID` forwarding support between reconnect attempts;
    - heartbeat/comment suppression for SSE events;
    - dedupe of already delivered JSON-RPC payloads across reconnect.
  - Tests expanded in `test/pincer/connectors/mcp/transports/http_test.exs`:
    - ignore heartbeat/comment frames;
    - reconnect on interrupted stream with backoff and dedupe;
    - stop with explicit error after max reconnect attempts.
  - Validation:
    - focused MCP suites green (`14 tests`);
    - full suite green (`mix test` => `3 doctests, 333 tests, 0 failures`).
  - Backlog/docs sync:
    - `TODO.md` marks `SPR-046` as done and marks MCP HTTP/SSE support as delivered;
    - `docs/capability_matrix.md` and `docs/SPECS/CAPABILITY_CHECKLIST_V1.md` now mark `C15` as `done`;
    - `CHANGELOG.md` updated with long-lived stream resilience notes.
- Implemented `SPR-047` (`C05`) with doc-first + TDD:
  - Added spec section in `SPECS.md`:
    - `Resiliência concorrente de callbacks/interactions (SPR-047 / C05)`.
  - Added RED/GREEN coverage:
    - `test/pincer/channels/telegram_test.exs`
      - flood de callbacks malformados em único poll sem crash do `UpdatesProvider`.
    - `test/pincer/channels/discord_test.exs`
      - flood de interactions malformadas sem `id/token` sem chamada indevida à API Discord.
    - `test/pincer/llm/reactive_client_test.exs`
      - troca concorrente de modelo durante backoff com semântica last-write-wins.
  - Runtime hardening:
    - `lib/pincer/channels/discord.ex`
      - `send_interaction_response/2` agora valida `interaction id/token` e ignora envelopes inválidos com warning.
    - `lib/pincer/llm/client.ex`
      - backoff agora drena mensagens `{:model_changed, ...}` e aplica a troca mais recente antes de retry imediato.
  - Validation:
    - focused suites green (`45 tests, 0 failures`);
    - full suite green (`mix test` => `3 doctests, 336 tests, 0 failures`).
  - Backlog/docs sync:
    - `TODO.md` marks `SPR-047` as done and closes the concurrent resilience checklist item;
    - `docs/capability_matrix.md` updates `C05` to include `SPR-047` delivery and clears immediate capability sprint queue;
    - `CHANGELOG.md` updated with concurrency resilience changes.
- Implemented `SPR-048` (`C17`) with doc-first + TDD:
  - Added spec section in `SPECS.md`:
    - `Streaming incremental consistente por SessionScope (SPR-048 / C17)`.
  - Core/channel runtime hardening:
    - `lib/pincer/channels/telegram/session.ex`
      - added `ensure_started/2` with dynamic `session_id` bind/rebind;
      - worker now supports topic rebinding + streaming state reset.
    - `lib/pincer/channels/discord/session.ex`
      - added `ensure_started/2` with dynamic `session_id` bind/rebind;
      - worker now supports topic rebinding + streaming state reset.
    - `lib/pincer/channels/telegram.ex`
      - message path now binds session worker with effective `session_id` resolved by policy.
    - `lib/pincer/channels/discord.ex`
      - message path now binds session worker with effective `session_id` resolved by policy.
    - `lib/pincer/infra/pubsub.ex`
      - added `unsubscribe/1` helper for safe topic rebinding.
    - supervisors accept explicit `session_id` on worker bootstrap:
      - `lib/pincer/channels/telegram/session_supervisor.ex`
      - `lib/pincer/channels/discord/session_supervisor.ex`
  - Tests added/updated:
    - `test/pincer/channels/telegram_session_test.exs`
      - rebind para `telegram_main` com entrega de `agent_response` no tópico novo.
    - `test/pincer/channels/discord_session_test.exs`
      - rebind para `discord_main` com entrega de `agent_response` no tópico novo.
  - Validation:
    - focused session/smoke suites green (`8 tests, 0 failures`);
    - full suite green (`mix test` => `3 doctests, 338 tests, 0 failures`).
  - Backlog/docs sync:
    - `TODO.md` marks `SPR-048` as done and closes item de streaming incremental consistente;
    - `CHANGELOG.md` updated with SessionScope streaming consistency notes.

- Security surface reduction and sidecar strategy update (2026-02-22 evening):
  - Removed legacy Node MCP sidecar PoC from repository:
    - deleted `mcp_sidecar/` tracked files from project path (local copy kept recoverable under `trash/`).
  - Removed ad-hoc sidecar test scripts tied to that PoC:
    - `test/test_sidecar_mcp.exs`
    - `test/test_10_skills.exs`
  - Added future-safe reintroduction contract:
    - `docs/SPECS/SIDECAR_RUNTIME_HARDENED_V2.md`
    - synchronized in `SPECS.md`, `TODO.md`, and `CHANGELOG.md`.
  - Validation:
    - full suite green after removal (`mix test` => `3 doctests, 356 tests, 0 failures`).
