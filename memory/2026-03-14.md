## 2026-03-14

- `tool_only` foi endurecido para colecoes GitHub/MCP grandes: `list_issues`, `list_prs`, `list_commits`, `search_code` e `list_repos` agora geram resumo curto e semantico em vez de preview cru de JSON.
- A politica pura `Pincer.Core.ToolResultSummary` passou a resumir arrays/objetos grandes de GitHub/MCP, limitando itens para nao voltar a despejar dump no canal.
- Ha regressao end-to-end em `test/pincer/core/executor_streaming_test.exs` provando que um turno com `list_issues` bem-sucedido seguido de final vazio ainda responde algo util ao usuario.
- Cobertura adicional travou `list_prs`, `search_code` e `list_repos` no helper puro, e um segundo caso end-to-end prova fallback util para `search_code` apos final vazio.
- `Pincer.Core.EmptyResponseRecoveryPolicy` teve ajuste de compatibilidade de compilacao para Elixir 1.18: regexes sairam de atributo de modulo e foram para helper puro, resolvendo falha no build Docker.
- O fluxo de `empty_response` agora segue o padrao do Nullclaw: saiu o gate por regex de smalltalk e entrou uma unica segunda tentativa explicita com instrucao contratual exigindo resposta visivel ou tool call.
- Corrigido bug do recovery explicito: `extract_xml_tool_calls/1` estava removendo `</thinking>` como stray closer, transformando reasoning fechado em tag orfa e apagando tambem a resposta visivel no `strip_reasoning/1`.
