# 2026-02-23

- Implementado `SPR-079` (orquestração multi-agente adaptativa em `/project`) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de wizard de projeto + board por sessão.
  - Novo módulo:
    - `lib/pincer/core/project_orchestrator.ex`
    - estado por sessão em ETS, com fases:
      - objetivo
      - tipo (`software` ou `nao-software`)
      - escopo
      - critério de sucesso
  - Geração de plano multi-agente ao concluir wizard:
    - `Architect`, `Coder`, `Reviewer`.
    - trilha adaptativa:
      - software: fluxo DDD/TDD
      - nao-software: fluxo Research/Validation
  - Integração de canais:
    - Telegram:
      - `/project` inicia/continua wizard;
      - mensagens livres durante coleta são capturadas pelo wizard;
      - `/kanban` prioriza board da sessão e cai para `TODO.md` no fallback.
    - Discord:
      - `/project` (texto e slash) inicia wizard;
      - mensagens livres durante coleta são capturadas;
      - `/kanban` prioriza board da sessão com fallback.
  - Testes adicionados/atualizados:
    - `test/pincer/core/project_orchestrator_test.exs`
    - `test/pincer/channels/telegram_test.exs`
    - `test/pincer/channels/discord_test.exs`
  - Validação:
    - `mix test test/pincer/core/project_orchestrator_test.exs test/pincer/core/project_board_test.exs test/pincer/core/ux_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs` verde (`68 tests, 0 failures`).

- Implementado `SPR-078` (orientação explícita DDD/TDD no `/project`) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de visualização diferenciada (`/kanban` enxuto, `/project` orientado).
  - `Pincer.Core.ProjectBoard.render/1` expandido com `view: :kanban | :project`.
  - `view: :project` agora adiciona seções:
    - `DDD Checklist`;
    - `TDD Checklist`;
    - `Next Action`.
  - Integração de canais:
    - Telegram `/project` chama `ProjectBoard.render(view: :project)`;
    - Discord `/project` (texto + slash) chama `ProjectBoard.render(view: :project)`.
  - Testes adicionados/atualizados:
    - `test/pincer/core/project_board_test.exs`
    - `test/pincer/channels/telegram_test.exs`
    - `test/pincer/channels/discord_test.exs`
  - Validação:
    - `mix test test/pincer/core/project_board_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs` verde (`54 tests, 0 failures`).

- Implementado `FIX-078` (kanban no runtime Docker com `TODO.md` presente):
  - `SPECS.md` atualizado com contrato de container para `/kanban`/`/project`.
  - `Dockerfile` atualizado:
    - builder copia `TODO.md`;
    - runtime copia `TODO.md` do builder para `/app/TODO.md`.
  - Validação operacional:
    - `docker compose build` + `docker compose up -d`;
    - dentro do container: `ls -l /app/TODO.md` (arquivo presente);
    - `mix run -e "IO.puts(Pincer.Core.ProjectBoard.render())"` retorna board em vez de fallback de arquivo ausente.

- Implementado `SPR-077` (Kanban operacional por comando) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para `/kanban` e `/project` (alias v1) com contexto DDD/TDD.
  - Novo módulo:
    - `lib/pincer/core/project_board.ex`
    - API: `Pincer.Core.ProjectBoard.render/1` (board textual a partir de checklist em `TODO.md`).
  - `Pincer.Core.UX` expandido:
    - comandos adicionados: `kanban`, `project`;
    - atalhos adicionados: `kanban`/`project`/`projeto` (com/sem `/`);
    - `help_text/1` e hints atualizados.
  - Integração de canais:
    - Telegram: `handle_command` com `/kanban` + alias `/project`;
    - Discord: slash e texto para `/kanban` + alias `/project`.
  - Testes adicionados/atualizados:
    - `test/pincer/core/project_board_test.exs`
    - `test/pincer/core/ux_test.exs`
    - `test/pincer/channels/telegram_test.exs`
    - `test/pincer/channels/discord_test.exs`
  - Validação:
    - `mix test test/pincer/core/project_board_test.exs test/pincer/core/ux_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs test/security_audit_test.exs` verde (`102 tests, 0 failures`).

- Implementado `FIX-077` (auto-envio de markdown para o usuário no ciclo de tools do executor) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para auto-publicação de artefatos `.md`.
  - `Pincer.Core.Executor` expandido:
    - snapshot inicial de markdown no início do ciclo;
    - detecção incremental pós-tool de arquivos `.md` novos/modificados;
    - envio automático de status ao usuário com path relativo + conteúdo (com truncamento seguro).
    - fail-safe: tracker não derruba execução em caso de erro de leitura/snapshot.
  - Testes adicionados/atualizados:
    - `test/pincer/core/executor_hex_test.exs` (envio automático e ausência de reenvio sem mudança).
  - Validação:
    - `mix test test/pincer/core/executor_hex_test.exs` verde (`6 tests, 0 failures`);
    - regressão complementar com `test/pincer/core/executor_streaming_test.exs` verde.

- Implementado `SPR-076` (integração AutoClaude v3 no SafeShell: runners + scripts locais) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para:
    - `yarn/pnpm/bun run <script>` a partir de `package.json`;
    - execução dinâmica de `./<script>.sh|.bash` no root do workspace.
  - `Pincer.Core.Tooling.CommandProfile` expandido:
    - `package_script_prefixes/1` agora emite `npm|yarn|pnpm|bun run <script>`;
    - `root_shell_script_prefixes/1` detecta scripts locais seguros do root;
    - normalização de script names endurecida (allowlist de caracteres, sem path separators).
  - Testes adicionados/atualizados:
    - `test/pincer/core/tooling/command_profile_test.exs`
    - `test/pincer/tools/safe_shell_dynamic_profile_test.exs`
  - Validação:
    - `mix test test/pincer/core/tooling/command_profile_test.exs test/pincer/tools/safe_shell_dynamic_profile_test.exs` verde;
    - `mix test test/security_audit_test.exs` verde (`44 tests, 0 failures`).

- Implementado `SPR-075` (integração AutoClaude v2 no SafeShell: scripts dinâmicos) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para allowlist dinâmica de scripts reais do workspace.
  - `Pincer.Core.Tooling.CommandProfile` expandido:
    - detecção de scripts `npm run <script>` a partir de `package.json` (`scripts`);
    - detecção de targets `make <target>` a partir de `Makefile`;
    - parsing fail-safe para JSON malformado/ausência de arquivos;
    - dedupe de prefixes dinâmicos.
  - `SafeShell` integrou automaticamente o novo perfil por reaproveitar `dynamic_command_prefixes/1`.
  - Testes adicionados/atualizados:
    - `test/pincer/core/tooling/command_profile_test.exs`
    - `test/pincer/tools/safe_shell_dynamic_profile_test.exs`
  - Validação:
    - `mix test test/pincer/core/tooling/command_profile_test.exs test/pincer/tools/safe_shell_dynamic_profile_test.exs`
    - `mix test test/security_audit_test.exs`
    - ambos verdes.

- Implementado `SPR-074` (integração AutoClaude v1 no SafeShell) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para perfil dinâmico de comandos por stack detectada.
  - Novo módulo:
    - `lib/pincer/core/tooling/command_profile.ex`
    - APIs: `detect_stacks/1`, `dynamic_command_prefixes/1`.
  - `Pincer.Tools.SafeShell` expandido:
    - fallback dinâmico após whitelist estático via `validate_dynamic_command/2`;
    - comandos dinâmicos v1 por stack:
      - Elixir: `mix format`, `mix pincer.security_audit`, `mix pincer.doctor`
      - Node: `npm test`
      - Rust: `cargo test`, `cargo check`
      - Python: `pytest`
    - validação de args/path reaproveita `unsafe_generic_arg?/2` (postura fail-closed preservada).
  - Testes adicionados:
    - `test/pincer/core/tooling/command_profile_test.exs`
    - `test/pincer/tools/safe_shell_dynamic_profile_test.exs`
  - Backlog:
    - `TODO.md` atualizado com `SPR-074` marcado como concluído.
  - Validação:
    - `mix test test/pincer/core/tooling/command_profile_test.exs test/pincer/tools/safe_shell_dynamic_profile_test.exs`
    - `mix test test/security_audit_test.exs`
    - ambos verdes.

- Implementado `FIX-076` (`/models` orientado a `config.yaml`) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para fonte de dados de `/models`:
    - priorizar `Application.get_env(:pincer, :llm)` (estrutura de `config.yaml`);
    - ignorar chave seletora `llm.provider`;
    - fallback para `:llm_providers` quando `:llm` estiver ausente/inválido/vazio.
  - `Pincer.LLM.Client` atualizado:
    - `list_providers/0` e `list_models/1` agora usam registry de seleção baseado em `:llm` com fallback legado.
  - Testes adicionados/atualizados:
    - `test/pincer/llm/client_test.exs` (precedência de `:llm`, ignorar `provider`, leitura de `model_list`, fallback para `:llm_providers`).
  - Validação:
    - `mix test test/pincer/llm/client_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs` verde (`56 tests, 0 failures`).

- Implementado `FIX-074` (robustez de histórico de tools + cooldown fail-safe) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para:
    - enriquecer `assistant.tool_calls` com `type: "function"` quando ausente em deltas de streaming;
    - leitura fail-safe de configs `:llm_retry`, `:llm_cooldown` e `:auth_profile_cooldown` em listas não-keyword.
  - `Pincer.Core.Executor` atualizado:
    - normaliza/enriquece `tool_calls` antes de reencadear o loop;
    - preserva compatibilidade de histórico para providers que exigem `tool_calls[*].type`.
  - `Pincer.LLM.Client`, `Pincer.Core.LLM.CooldownStore` e `Pincer.Core.AuthProfiles` endurecidos:
    - fallback seguro para listas não-keyword sem explodir `Keyword.get/3`.
  - Testes adicionados/atualizados:
    - `test/pincer/core/executor_hex_test.exs` (regressão de `tool_calls.type` no histórico);
    - `test/pincer/llm/retry_policy_test.exs` (erro terminal 400 com config malformed sem `FunctionClauseError`).
  - Validação:
    - `mix test test/pincer/core/executor_hex_test.exs test/pincer/llm/retry_policy_test.exs`;
    - `mix test test/pincer/core/auth_profiles_test.exs test/pincer/core/llm/cooldown_store_test.exs test/pincer/llm/client_auth_profiles_test.exs test/pincer/llm/failover_integration_test.exs`;
    - `mix test` verde (`3 doctests, 445 tests, 0 failures`).

- Implementado `SPR-052` (front de segurança) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato e critérios de aceite para:
    - sanitização anti-hidden/invisible no `Web`;
    - bloqueio multiline/line-continuation no `SafeShell`;
    - expansão do `SecurityAudit` para flags perigosas.
  - `Pincer.Tools.WebVisibility` criado:
    - `sanitize_html/1` remove comentários HTML e nós ocultos comuns;
    - `strip_invisible_unicode/1` remove caracteres Unicode invisíveis usados em obfuscação.
  - `Pincer.Tools.Web` integrado ao novo pipeline de visibilidade/sanitização.
  - `Pincer.Tools.SafeShell` endurecido para exigir aprovação em payload multiline/line-continuation.
  - `Pincer.Core.SecurityAudit` expandido com checks de dangerous flags:
    - `gateway.control_ui.allow_insecure_auth=true`
    - `gateway.control_ui.dangerously_disable_device_auth=true`
    - `hooks.gmail.allow_unsafe_external_content=true`
    - `hooks.mappings[*].allow_unsafe_external_content=true`
    - `tools.exec.apply_patch.workspace_only=false`
- Testes adicionados/atualizados:
  - `test/pincer/tools/web_visibility_test.exs`
  - `test/security_audit_test.exs` (regressões SafeShell multiline)
  - `test/pincer/core/security_audit_test.exs` (dangerous flags)
- Validação:
  - `mix test test/pincer/tools/web_visibility_test.exs test/security_audit_test.exs test/pincer/core/security_audit_test.exs test/mix/tasks/pincer.security_audit_test.exs`
  - Resultado: `53 tests, 0 failures`.

- Implementado `SPR-053` (`restrict_to_workspace` v1) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de runtime para:
    - guard centralizado de path confinement;
    - revalidação pós-aprovação no executor (fail-closed);
    - auditoria de postura para `tools.restrict_to_workspace`.
  - Novo módulo:
    - `lib/pincer/core/workspace_guard.ex`
    - API principal: `Pincer.Core.WorkspaceGuard.confine_path/2`.
  - Refactors de hardening:
    - `Pincer.Tools.FileSystem` migrado para usar `WorkspaceGuard`.
    - `Pincer.Tools.SafeShell`:
      - valida path args com `WorkspaceGuard` (inclui bloqueio de escape via symlink);
      - adiciona `approved_command_allowed?/2` para revalidação no fluxo de aprovação.
    - `Pincer.Core.Executor`:
      - aprovações de `run_command` agora passam por revalidação de workspace;
      - comandos reprovados retornam erro explícito e não executam.
    - `Pincer.Core.SecurityAudit`:
      - novo check `tools_restrict_workspace`;
      - `tools.restrict_to_workspace=false` é erro bloqueante.
  - Config baseline:
    - `config.yaml` agora inclui `tools.restrict_to_workspace: true`.
- Testes adicionados/atualizados:
  - `test/pincer/core/workspace_guard_test.exs`
  - `test/pincer/core/executor_hex_test.exs` (bloqueio pós-aprovação fora da política)
  - `test/security_audit_test.exs` (regressão SafeShell com symlink escape)
  - `test/pincer/core/security_audit_test.exs` (erro com `tools.restrict_to_workspace=false`)
- Validação:
  - `mix test` => `3 doctests, 367 tests, 0 failures`.
  - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-054` (sidecar hardened baseline) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato explícito de isolamento para `skills_sidecar`.
  - Novo módulo:
    - `lib/pincer/connectors/mcp/skills_sidecar_policy.ex`
    - API principal: `Pincer.Connectors.MCP.SkillsSidecarPolicy.validate/1`.
  - Política fail-closed para sidecar:
    - exige `docker run`;
    - exige flags mínimas: `--read-only`, `--network=none`, `--cap-drop=ALL`, `--pids-limit`, `--memory`, `--cpus`, `--user` não-root, mount `-v ...:/sandbox`.
  - Integração:
    - `Pincer.Connectors.MCP.Manager.resolve_servers_config/2` agora remove `skills_sidecar` inseguro da config resolvida.
    - `Pincer.Core.SecurityAudit` ganhou check `mcp_skills_sidecar_isolation`.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`policy + manager + audit`) verde: `20 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 376 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-055` (sidecar execution audit) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de auditoria por execução no `skills_sidecar`.
  - Novo módulo:
    - `lib/pincer/connectors/mcp/sidecar_audit.ex`
    - APIs:
      - `Pincer.Connectors.MCP.SidecarAudit.status_from_result/1`
      - `Pincer.Connectors.MCP.SidecarAudit.emit/5`
  - Integração no manager:
    - `Pincer.Connectors.MCP.Manager.audit_sidecar_result/5` (helper `@doc false`);
    - `handle_call({:execute, ...})` agora audita chamadas roteadas para `skills_sidecar` sem alterar retorno funcional.
  - Contrato de auditoria emitido:
    - evento `[:pincer, :mcp, :skills_sidecar, :tool_execution]`;
    - métricas/metadados mínimos: `duration_ms`, `tool`, `skill_id`, `skill_version`, `status`.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/sidecar_audit_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs` (helpers de audit sidecar)
  - Validação:
    - suíte focada (`skills_sidecar_policy + sidecar_audit + manager + security_audit`) verde: `24 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 380 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-056` (sidecar env secrets denylist) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de bloqueio de env sensível em `skills_sidecar`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` expandido:
    - novo `sensitive_env_keys/0` (denylist explícita);
    - parsing de env em formatos `map`, lista de tuplas e `KEY=VALUE`;
    - validação fail-closed com erro `{:sensitive_env_keys_blocked, keys}`.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` já reaproveita a policy e agora remove sidecar também quando env sensível é detectado;
    - `SecurityAudit` (via policy) passa a reportar sidecar inseguro também para segredo em env.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + sidecar_audit + manager + security_audit`) verde: `30 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 386 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-057` (sidecar tool timeout hard) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de timeout hard para `skills_sidecar`.
  - `Pincer.Connectors.MCP.Manager` expandido:
    - novo helper `call_tool_with_timeout/4`;
    - `skills_sidecar` agora executa tool call via `Task.yield` com timeout e shutdown hard;
    - timeout retorna `{:error, :timeout}` (compatível com auditoria existente);
    - servidores não-sidecar mantêm fluxo sem timeout hard extra.
  - Integração:
    - `handle_call({:execute, ...})` passou a usar `call_tool_with_timeout/4` antes de normalizar resposta.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/manager_test.exs` (sucesso no prazo, timeout, bypass para não-sidecar).
  - Validação:
    - suíte focada (`skills_sidecar_policy + sidecar_audit + manager + security_audit`) verde: `33 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 389 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-058` (sidecar mount target allowlist) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de allowlist para targets de mount em `skills_sidecar`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - validação de targets permitidos para mounts (`/sandbox`, `/tmp`);
    - bloqueio fail-closed para targets não permitidos via `{:disallowed_mount_targets, targets}`.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` já reaproveita a policy e passa a remover sidecar também quando mount target é inválido;
    - `SecurityAudit` (via policy) passa a reportar erro para sidecar com mount target fora da allowlist.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + sidecar_audit + manager + security_audit`) verde: `37 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 393 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-059` (sidecar dangerous docker flags denylist) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de denylist de flags Docker perigosas no `skills_sidecar`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - validação fail-closed para flags de alto risco:
      - `--privileged`
      - `--cap-add`
      - `--device`
      - `--pid=host`
      - `--ipc=host`
      - `--security-opt=*unconfined*`
    - erro explícito: `{:dangerous_docker_flags, flags}`.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com flags perigosas;
    - `SecurityAudit` (via policy) passa a reportar erro para sidecar com flags perigosas.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + sidecar_audit + manager + security_audit`) verde: `40 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 396 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-060` (sidecar image digest pinning) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de pinning por digest para imagem do `skills_sidecar`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - erro explícito para imagem ausente: `:image_required`;
    - erro explícito para imagem não-pinada: `:unpinned_image_digest`;
    - validação aceita somente formato `repo@sha256:<64-hex>`.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com imagem não-pinada;
    - `SecurityAudit` (via policy) passa a reportar erro para sidecar com imagem não-pinada.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - `mix test` verde: `3 doctests, 399 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-061` (sandbox mount source confinement) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de confinamento de source para mounts que apontam para `/sandbox`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - novo erro explícito: `{:invalid_sandbox_mount_sources, sources}`;
    - mounts com target `/sandbox` agora rejeitam:
      - source absoluto (`/etc:/sandbox`);
      - source volume nomeado (`pincer-skills:/sandbox`);
      - source com `..` (traversal).
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com source inválido em `/sandbox`;
    - `SecurityAudit` (via policy) passa a reportar erro para source inválido em `/sandbox`.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + manager + security_audit`) verde: `45 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 403 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-062` (tmp mount source guard) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de bloqueio de source path para mounts que apontam para `/tmp`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - novo erro explícito: `{:invalid_tmp_mount_sources, sources}`;
    - mounts com target `/tmp` agora aceitam apenas source de volume nomeado;
    - mounts com source path (absoluto/relativo/traversal) em `/tmp` são bloqueados.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com source inválido em `/tmp`;
    - `SecurityAudit` (via policy) passa a reportar erro para source inválido em `/tmp`.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + manager + security_audit`) verde: `48 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 406 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-063` (env args secret guard) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de bloqueio de secrets também para `-e/--env` em `docker args`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - parser de env args Docker (`-e`, `--env=`, `--env KEY=VALUE`);
    - denylist sensível agora unifica chaves vindas de `env` no config e de args Docker;
    - bypass por args foi fechado com erro explícito `{:sensitive_env_keys_blocked, keys}`.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com secrets em args;
    - `SecurityAudit` (via policy) passa a reportar erro para secrets em args.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + manager + security_audit`) verde: `52 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 410 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-064` (mount flag bypass guard) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de bloqueio explícito de `--mount`/`--mount=...`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - `--mount` entrou na denylist de flags perigosas;
    - bloqueio explícito evita bypass da política de mounts baseada em `-v/--volume`.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com `--mount`;
    - `SecurityAudit` (via policy) passa a reportar erro para sidecar com `--mount`.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + manager + security_audit`) verde: `55 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 413 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-065` (env-file flag guard) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de bloqueio explícito de `--env-file`/`--env-file=...`.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - `--env-file` entrou na denylist de flags perigosas;
    - bloqueio explícito evita ingestão indireta de segredos do host via arquivo de ambiente.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com `--env-file`;
    - `SecurityAudit` (via policy) passa a reportar erro para sidecar com `--env-file`.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Validação:
    - suíte focada (`skills_sidecar_policy + manager + security_audit`) verde: `58 tests, 0 failures`;
    - `mix test` verde: `3 doctests, 413 tests, 0 failures`;
    - `set -a; source .env; set +a; mix pincer.security_audit --strict` => `status: ok`.

- Implementado `SPR-066` (required flag override guard) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de validação por valor efetivo (última ocorrência) de flags obrigatórias.
  - `Pincer.Connectors.MCP.SkillsSidecarPolicy` endurecido:
    - `flag_value/2` passou a usar semântica de última ocorrência para flags com valor;
    - bypass por override tardio inseguro (ex.: `--network=none ... --network=host`) foi fechado.
  - Integração:
    - `MCP.Manager.resolve_servers_config/2` reaproveita a policy e remove sidecar com override final inseguro;
    - `SecurityAudit` (via policy) passa a reportar erro para override final inseguro.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Backlog:
    - removidos do `TODO.md` os itens de embeddings local e transcrição de voz (corte solicitado pelo usuário).
  - Validação:
    - suíte focada (`skills_sidecar_policy + manager + security_audit`) verde: `61 tests, 0 failures`;
    - `mix test` e `mix pincer.security_audit --strict` executados e verdes após fechamento da sprint.

- Implementado `SPR-067` (CLI history persistence) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de histórico persistente no `mix pincer.chat`.
  - Novo módulo:
    - `lib/pincer/cli/history.ex`
    - APIs: `path/1`, `append/2`, `recent/2`, `clear/1`.
  - Integração:
    - `Pincer.CLI.process_command/1` agora aceita:
      - `/history`
      - `/history N`
      - `/history clear`
    - `Pincer.CLI.loop/1` passou a:
      - listar histórico persistido;
      - limpar histórico sob comando;
      - persistir automaticamente cada input enviado ao backend.
  - Testes adicionados/atualizados:
    - `test/pincer/cli/history_test.exs`
    - `test/pincer/cli_test.exs`
  - Backlog:
    - item `CLI Interativo (mix pincer.chat com histórico persistente)` marcado como concluído em `TODO.md`.

- Implementado `SPR-068` (webhook universal v1) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de ingestão receive-only para canal webhook.
  - Novo módulo:
    - `lib/pincer/channels/webhook.ex`
    - API pública: `Pincer.Channels.Webhook.ingest/2`.
  - Hardening aplicado:
    - `token_env` obrigatório para start (fail-closed);
    - validação de token por request em `ingest/2`;
    - dedupe de retries por `event_id`.
  - Roteamento:
    - texto extraído de payloads em formatos comuns (`text`, `content`, `prompt`, nested);
    - sessão resolvida por `session_id` explícito ou por `source + sender_id`;
    - fallback para `default_session_id`.
  - Segurança:
    - `SecurityAudit` agora exige `token_env` também para canal `webhook` quando habilitado.
  - Testes adicionados/atualizados:
    - `test/pincer/channels/webhook_test.exs`
    - `test/pincer/core/security_audit_test.exs`

- Implementado `SPR-069` (smart sub-agent progress notifications) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de notificações inteligentes para progresso de sub-agente.
  - Novo módulo:
    - `lib/pincer/core/sub_agent_progress.ex`
    - API: `Pincer.Core.SubAgentProgress.notifications/2`.
  - Integração no runtime:
    - `Session.Server` heartbeat agora:
      - publica `agent_status` determinístico para updates reconhecidos de blackboard;
      - evita spam com dedupe por agente/etapa;
      - chama fallback LLM (`evaluate_blackboard_update`) apenas para updates ambíguos.
    - trigger agendado publica início de sub-agente como `agent_status`.
    - `Telegram.Session` passou a entregar `agent_status` como mensagem visível.
  - Testes adicionados/atualizados:
    - `test/pincer/core/sub_agent_progress_test.exs`
    - `test/pincer/channels/telegram_session_test.exs`
  - Backlog:
    - item `Notificações inteligentes de progresso do Sub-agente` marcado como concluído em `TODO.md`.

- Implementado `SPR-070` (processamento de imagens/logs via arquivo no Telegram) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de ingestão de anexos Telegram e resolução segura `telegram://file/...` no executor.
  - Telegram channel:
    - `Pincer.Channels.Telegram.API` expandido com `get_file/1`;
    - `UpdatesProvider` ganhou `prepare_input_content/2` para montar payload multimodal com `attachment_ref`;
    - updates com `photo`/`document` agora deixam de ser ignorados e passam a ser roteados para sessão.
  - Executor:
    - novo `Pincer.Core.Executor.resolve_attachment_url/2`;
    - `default_file_fetch/1` resolve URL Telegram em runtime sem persistir token no histórico;
    - fallback para `text/*` em provider sem suporte a arquivos agora converte attachment em texto.
  - Testes adicionados/atualizados:
    - `test/pincer/channels/telegram_test.exs`
    - `test/pincer/core/executor_attachment_url_test.exs`
    - `test/pincer/multimodal/attachment_integration_test.exs`
  - Validação:
    - suíte focada (telegram + executor URL + multimodal) verde;
    - `mix test` verde: `3 doctests, 440 tests, 0 failures`;
    - `mix pincer.security_audit --strict` verde (`status: ok`).

- Implementado `SPR-071` (sidecar v2 checksum + auditoria enriquecida) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato de checksum obrigatório para sidecar e metadata de auditoria por execução.
  - Hardening de policy:
    - `Pincer.Connectors.MCP.SkillsSidecarPolicy` agora exige `artifact_checksum`/`skill_artifact_checksum`;
    - validação fail-closed para checksum ausente (`:artifact_checksum_required`) e malformado (`:invalid_artifact_checksum`).
  - Auditoria de execução:
    - `Pincer.Connectors.MCP.Manager.audit_sidecar_result/6` agora propaga `skill_id`, `skill_version` e `artifact_checksum` quando presentes nos argumentos.
  - Testes adicionados/atualizados:
    - `test/pincer/connectors/mcp/skills_sidecar_policy_test.exs`
    - `test/pincer/connectors/mcp/manager_test.exs`
    - `test/pincer/core/security_audit_test.exs`
  - Backlog:
    - item `Sidecar de skills v2 hardened` marcado como concluído em `TODO.md`.

- Implementado `SPR-072` (containerização do servidor) com doc-first:
  - `SPECS.md` atualizado com contrato de runtime Docker v1 para servidor.
  - Artefatos adicionados:
    - `Dockerfile` multi-stage para build/runtime do Pincer;
    - `.dockerignore` para reduzir contexto de build;
    - `docker-compose.yml` com serviço `pincer-server` e volumes persistentes (`db/`, `logs/`, `sessions/`, `memory/`).
  - Hardening/operação:
    - runtime não-root com UID/GID configuráveis por build args (default `1000:1000`);
    - cópia de `.mix`/`.hex` para runtime não-root evitar loop de restart por falta de Hex;
    - entrypoint `infrastructure/docker/entrypoint.sh`:
      - inicia `epmd`;
      - executa `mix ecto.migrate`;
      - inicia `mix pincer.server`.
  - Compatibilidade:
    - adicionado `config/prod.exs` (necessário para `MIX_ENV=prod` em container);
    - `Pincer.Tools.WebVisibility` ajustado para compilar no Elixir 1.18 sem injeção inválida de atributo regex.
  - Validação:
    - `mix test test/pincer/tools/web_visibility_test.exs` verde;
    - `MIX_ENV=prod mix compile` verde;
    - `docker compose build pincer-server` verde;
    - `docker compose up -d --force-recreate pincer-server` verde;
    - logs confirmam `Migrations already up` + Telegram poller ativo.
  - Observação:
    - MCP `filesystem`/`github` no container continuam indisponíveis por ausência de `npx` (`:enoent`), sem derrubar o servidor principal.

- Implementado `SPR-073` (robustez de tool-calls + UX Telegram native-first) com doc-first + TDD:
  - `SPECS.md` atualizado com contrato para:
    - normalização robusta de `tool_calls.function.arguments` no executor;
    - remoção do teclado persistente duplicado no Telegram por padrão.
  - Executor hardening:
    - `lib/pincer/core/executor.ex` agora aceita argumentos de tool call como JSON string, mapa já decodificado ou vazio;
    - merge de deltas de tool call agora suporta fragmentos não-binários sem crash;
    - parsing de tool call tolera chaves string/atom e faz fallback fail-soft para payload inválido.
  - Telegram UX:
    - `lib/pincer/channels/telegram.ex` `menu_reply_markup/0` agora usa modo native-first (`remove_keyboard: true`) por padrão;
    - suporte de compatibilidade para reativar teclado legado via `channels.telegram.menu_keyboard: "persistent"`.
  - Testes adicionados/atualizados:
    - `test/pincer/core/executor_hex_test.exs` (arguments em mapa no fluxo de tool use);
    - `test/pincer/channels/telegram_test.exs` (markup native-first e regressões de fallback com `menu_reply_markup/0`).
  - Validação:
    - `mix test test/pincer/core/executor_hex_test.exs test/pincer/channels/telegram_test.exs` verde;
    - `mix test test/pincer/channels/discord_test.exs test/pincer/channels/discord_session_test.exs test/pincer/core/executor_hex_test.exs test/pincer/channels/telegram_test.exs` verde;
    - `mix test` verde (`3 doctests, 444 tests, 0 failures`).

- Implementado `FIX-075` (pairing persistente + fluxo out-of-band) com doc-first + TDD:
  - `Pincer.Core.Pairing` agora persiste `pending/pairs` em DETS (`sessions/pairing_store.dets`) e reidrata ETS no bootstrap.
  - `AccessPolicy` em modo `pairing` nao exibe mais codigo de 6 digitos no canal bloqueado; orienta solicitar codigo ao operador e usar `/pair <codigo>`.
  - comandos `/pair` de Telegram/Discord atualizados para fluxo out-of-band (solicitar novo codigo ao operador em `not_pending`, `expired` e `attempts_exceeded`).
  - emissao out-of-band de codigo no pairing:
    - log estruturado `[PAIRING] ... code=...`
    - broadcast PubSub para `session:cli:admin` com payload do codigo.
  - Testes:
    - `test/pincer/core/pairing_test.exs` adiciona persistencia apos recriacao de ETS.
    - `test/pincer/core/access_policy_test.exs` garante ausencia de codigo no deny message e aprovacao via `Pairing.issue_code/3`.
  - Validacao:
    - `mix test test/pincer/core/pairing_test.exs test/pincer/core/access_policy_test.exs`
    - `mix test test/pincer/core/pairing_test.exs test/pincer/core/access_policy_test.exs test/pincer/channels/telegram_test.exs test/pincer/channels/discord_test.exs`
    - `mix test` verde (`3 doctests, 446 tests, 0 failures`).
