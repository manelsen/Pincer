## 2026-03-13

- Arquivos `SOUL.md` e `USER.md` nao existem na raiz do workspace ativo; apenas templates foram encontrados em `priv/pincer/templates/`.
- Leitura do roadmap em `docs/architecture/2026-03-13-architecture-boundaries-report.md` confirmou a estrategia: Phase 1 primeiro, sem apertar `boundary` ainda.
- Primeiro slice implementado do roadmap:
  - nova politica pura `Pincer.Core.StatusMessagePolicy`;
  - semantica de upsert de mensagem de status saiu das sessoes de Telegram/Discord e foi centralizada em `Core`;
  - fallback de transporte permaneceu nos canais.
- Especificacao do slice registrada em `SPECS.md` como `Status Message Policy v1`.
- Testes-alvo verdes:
  - `test/pincer/core/status_message_policy_test.exs`
  - `test/pincer/core/streaming_policy_test.exs`
  - `test/pincer/channels/telegram_session_test.exs`
  - `test/pincer/channels/discord_session_test.exs`
- Slice posterior do roadmap extraiu a formatacao de resposta degradada `tool-only` para `Pincer.Core.ToolOnlyOutcomeFormatter`.
- `Pincer.Core.TurnOutcomePolicy` agora so classifica `tool-only` como estado estruturado, sem string pronta de "sucesso".
- O `Executor` deixou de responder "✅ Concluido..." quando houve apenas tools sem resposta final do assistente.
- Interface do tool web leve foi dividida em `web_search` e `web_fetch` no registry nativo, mantendo compatibilidade interna do backend compartilhado em `Pincer.Adapters.Tools.Web`.
- Ajustes em `executor_hex_test` alinharam os testes ao contrato atual do executor para mocks em processo filho, approval flow e artefatos markdown na raiz do workspace.
- `browser` deixou de ser exposto ao modelo quando a infra de browser esta desabilitada; leitura simples de URL agora deve tender a `web_fetch`.
- `Executor` ganhou recuperacao leve para `empty_response` no primeiro turno: tenta `chat_completion` sem tools antes de devolver erro de provider vazio ao canal.
- `PromptAssembly` foi extraido do `Executor`, levando pruning e system-context augmentation para um seam dedicado em `Core`.
- `ChannelEventPolicy` centralizou classificacao de `agent_status` textual e envelope de `agent_error`.
- `WhatsApp.Session` passou a reutilizar `ProjectFlowDelivery` em vez de falar direto com `ProjectRouter` e `Session.Server`.
- Estado atual do roadmap: a Phase 1 do relatorio de boundaries foi funcionalmente concluida; o restante agora encaixa melhor como sharpening de Phase 2.
- Phase 2 foi aberta com um slice pequeno: `Pincer.Core.UX` agora e um sub-boundary explicito, com `MenuPolicy` e `ModelKeyboard` exportados por ele.
- `Pincer.Core` manteve re-export temporario desses modulos para compatibilidade enquanto `Channels` ainda aponta para o boundary ancestral.
- `empty_response` recovery agora so tenta `chat_completion` leve para smalltalk/greetings; perguntas factuais como "o que tem na pasta atual?" falham limpo em vez de alucinar conteudo.
- `web_fetch` agora classifica erro de TLS/timeout em mensagens curtas via `WebFetchError`, em vez de vazar dumps enormes de `Req.TransportError`.
- Adicionei regressao para turno pos-tool totalmente vazio: o comportamento esperado e responder com `tool_only`, nao com `:empty_response`.
- `web_fetch` agora tenta fallback seguro `https -> http` quando o erro e `hostname_check_failed`, para aproximar o comportamento do que o browser consegue resolver em dominios legados.
- `ToolAnswerPatternPolicy` entrou no grounding pos-tool para ensinar respostas factuais apos `git_inspect` e ferramentas GitHub/MCP como `get_issue` e `get_pr`.
- `git_inspect` agora classifica erros comuns (`not a git repository`, `pathspec`, `ambiguous argument`) em mensagens curtas via `GitInspectError`.
- O tool `github` agora classifica erros comuns de API/transporte (`401`, `403 rate limit`, `404`, timeout`) em mensagens curtas via `GitHubError`.
- `github.ex` ganhou seam de cliente HTTP injetavel por configuracao, permitindo testes reais de erro sem monkeypatch global de `Req`.
