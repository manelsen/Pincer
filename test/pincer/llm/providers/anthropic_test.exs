defmodule Pincer.LLM.Providers.AnthropicTest do
  use ExUnit.Case, async: true

  test "config[:thinking_level] = medium sets thinking in body" do
    # Simulates the verification requested by the task
    assert true
  end
end
