# Fixture Workspace

This workspace exists to exercise the `file_system` tool against realistic files.

- `lib/pipeline.ex` is used for hashline and anchored edit tests.
- `config/runtime.exs` is used for exact patch edits.
- `docs/runbook.md` is used for search and find tests.
